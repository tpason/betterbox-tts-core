# Windows + NVIDIA (CUDA 12.8): dòng --extra-index-url giúp pip lấy wheel GPU.
# macOS/Linux: pip vẫn cài đúng wheel từ PyPI chính (không có bản CUDA cho Mac).
# Nếu trên Mac gặp lỗi resolve torch: dùng requirements-mac.txt thay cho file này.
#
--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple

torch==2.10.0
torchaudio==2.10.0

safetensors>=0.7.0
huggingface_hub>=1.5.0,<2.0
git+https://github.com/huggingface/transformers.git
accelerate>=0.33.0
librosa==0.11.0
scipy>=1.10.0
soundfile>=0.12.0
numpy>=2.0.0,<3.0
gradio>=6.0.0
einops>=0.7.0
conformer==0.3.2
s3tokenizer
diffusers>=0.37.0
resemble-perth==1.0.1
omegaconf==2.3.0
pedalboard
pydub
datasets>=2.14.0
jiwer>=3.0.0
colorama>=0.4.0
tqdm>=4.65.0
pandas>=2.0.0
pyyaml>=6.0
requests>=2.31.0
beautifulsoup4>=4.12.0
playwright>=1.52.0
psycopg[binary]>=3.2.0

# Optional: browser-use local agent (fallback cho JS-heavy / phức tạp sites)
# pip install browser-use langchain-ollama
# playwright install chromium  (đã cài ở trên nếu dùng playwright)
# browser-use>=0.1.0
# langchain-ollama>=0.1.0
