# Copy to .env in this directory (optional — defaults work for local dev):
#   cp .env.example .env
#
# Production: set a strong password and update STORY_DATABASE_URL everywhere.

POSTGRES_PASSWORD=betterbox
